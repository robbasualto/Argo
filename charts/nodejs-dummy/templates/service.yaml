apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-dummy.fullname" . }}
  labels:
    {{- include "nodejs-dummy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-dummy.selectorLabels" . | nindent 4 }}
